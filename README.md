# sdk-dotnet

Taqlyn .NET server SDK — short-link create/manage (**stub — client not shipped yet**).

Expected env once the client lands:

```bash
export TAQLYN_BASE_URL=https://api.rutvik.qzz.io
export TAQLYN_CLIENT_ID=app_test_...
export TAQLYN_PRIVATE_KEY='-----BEGIN PRIVATE KEY-----...'
```

Demo placeholder: [`examples/server/dotnet`](../../examples/server/dotnet).
